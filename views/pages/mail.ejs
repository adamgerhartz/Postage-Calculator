<!DOCTYPE html>
<html lang="en">
<head>
	<%- include("../partials/header.ejs") %>
</head>
<body>
	<%- include("../partials/form.ejs") %>
	<%- 
		const weightSelection = [...document.getElementBById('weight').children]; 
		weightSelection.forEach((selection) => {
			selection.addEventListener('select', (event) => {
				console.log(event);
			});
		});
	%>
</body>
</html>